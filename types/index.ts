export interface datasetType {
    id: number;
    name: string;
    image: string;
}

export interface educationType {
    id: number;
    name: string;
    description: string;
    image: string;
}
